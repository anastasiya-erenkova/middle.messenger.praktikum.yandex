extends ../layouts/page.pug
include ../components/error.pug

block append head 
  link(rel="stylesheet", href="../styles/main.scss")
  title Ошибка

block main
  - const status = 500
  - const description = "Мы уже фиксим"

  main
    +error(status, description)