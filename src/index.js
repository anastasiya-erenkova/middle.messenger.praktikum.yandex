// import Handlebars from "handlebars";

// Handlebars.registerHelper("getName", function (fields) {
//   const field = fields.find((field) => field.name === "first_name");
//   return field ? field.value : null;
// });
